<Page
    x:Class="GentApp.Views.BranchPromotionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:local="using:GentApp.DataModel"
	xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
	xmlns:core="using:Microsoft.Xaml.Interactions.Core"
	mc:Ignorable="d"
    DataContext="{Binding BranchViewModelInstance, Source={StaticResource Locator}}">

	<interactivity:Interaction.Behaviors>
		<core:EventTriggerBehavior EventName="Loaded">
			<core:InvokeCommandAction Command="{Binding Path=LoadPromotionsCommand}" />
		</core:EventTriggerBehavior>
	</interactivity:Interaction.Behaviors>

	<StackPanel>
		<StackPanel Orientation="Horizontal" x:Name="horStackPanel">
			<TextBlock Text="Manage the promotions of" Margin="0,0,5,0"></TextBlock>
			<TextBlock Text="{Binding Name}"></TextBlock>
			<TextBlock Text=":" />
		</StackPanel>
		<TextBlock>Your current promotions:</TextBlock>
		<TextBlock x:Name="NoCurrentPromotionsTextBlock"></TextBlock>
		<ListView ItemsSource="{Binding Path=CurrentPromotions}" IsItemClickEnabled="True" ItemClick="PromotionsListView_ItemClick" x:Name="currentPromotionsListView" Margin="0,0,0,20">
			<ListView.ItemTemplate>
				<DataTemplate>
					<StackPanel>
						<TextBlock Text="{Binding Path=Title}" />
					</StackPanel>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>

		<!--<TextBlock>All your promotions:</TextBlock>
		<TextBlock x:Name="NoPromotionsTextBlock"></TextBlock>
		<ProgressRing x:Name="progressPromotions" HorizontalAlignment="Left" Margin="5,10,0,0" />
		<ListView ItemsSource="{Binding Path=SelectedBranch.Promotions}" IsItemClickEnabled="True" ItemClick="PromotionsListView_ItemClick" x:Name="promotionsListView">
			<ListView.ItemTemplate>
				<DataTemplate x:DataType="local:Promotion">
					<StackPanel>
						<TextBlock Text="{Binding Path=Title}" />
					</StackPanel>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>-->

		<TextBlock>Non current promotions:</TextBlock>
		<TextBlock x:Name="NoPromotionsTextBlock"></TextBlock>
		<ListView ItemsSource="{Binding Path=NonCurrentPromotions}" IsItemClickEnabled="True" ItemClick="PromotionsListView_ItemClick" x:Name="promotionsListView">
			<ListView.ItemTemplate>
				<DataTemplate x:DataType="local:Promotion">
					<StackPanel>
						<TextBlock Text="{Binding Path=Title}" />
					</StackPanel>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>

		<SymbolIcon Symbol="Add" IsTapEnabled="True" Tapped="AddIcon_Tapped"></SymbolIcon>
	</StackPanel>
</Page>
